using UnityEngine;
using System.Collections;

public class HideChildren : MonoBehaviour {

	// Use this for initialization
	void Start () {
		CurrentBezier.ToggleMeshes(transform);
	}	
	// Update is called once per frame
	void Update () {
	
	}
}
